# Сюда кладутся нативные библиотеки Vosk (в git не коммитятся):
#   libvosk.so   — Linux  (./scripts/setup-asr.sh)
#   libvosk.dll  — Windows (./scripts/fetch-vosk-windows.sh)
#   libvosk.lib  — Windows import lib
