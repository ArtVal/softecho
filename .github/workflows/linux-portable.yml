# Portable-сборка под Linux (x86_64), без установщика.
name: linux-portable

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - asr: false
            suffix: text
          - asr: true
            suffix: asr

    steps:
      - uses: actions/checkout@v4

      - name: Install system deps (UI + optional ALSA)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxi-dev \
            libgtk-3-dev \
            libssl-dev \
            pkg-config

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Check version matches tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          chmod +x scripts/version.sh
          ./scripts/version.sh check-tag

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-${{ matrix.suffix }}

      - name: Fetch Vosk Linux natives
        if: matrix.asr
        run: ./scripts/fetch-vosk-linux.sh

      - name: Build release (text)
        if: ${{ !matrix.asr }}
        run: cargo build --release

      - name: Build release (asr)
        if: matrix.asr
        run: cargo build --release --features asr

      - name: Package portable tar.gz
        env:
          SKIP_BUILD: "1"
        run: |
          set -euo pipefail
          chmod +x scripts/package-linux-portable.sh
          ARGS=
          if [[ "${{ matrix.asr }}" == "true" ]]; then
            ARGS=--asr
          fi
          ./scripts/package-linux-portable.sh $ARGS
          ls -la dist/

      - name: Upload portable folder
        uses: actions/upload-artifact@v4
        with:
          name: softecho-linux-x86_64-${{ matrix.suffix }}
          # Папка, не .tar.gz: иначе Actions даёт zip с tar.gz внутри.
          path: dist/softecho-linux-x86_64-${{ matrix.suffix }}/
          if-no-files-found: error

  release:
    name: github-release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: linux
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Pack tar.gz for Release
        run: |
          set -euo pipefail
          mkdir -p dist
          shopt -s nullglob
          for dir in artifacts/softecho-linux-x86_64-*/; do
            name="$(basename "$dir")"
            tar -czf "dist/${name}.tar.gz" -C artifacts "$name"
            ls -lh "dist/${name}.tar.gz"
          done
          test -n "$(ls -A dist)" || { echo "Нет архивов" >&2; exit 1; }
      - name: Attach to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          ls -la dist/
          shopt -s nullglob
          files=(dist/*.tar.gz)
          if [[ ${#files[@]} -eq 0 ]]; then
            echo "Нет tar.gz в dist/" >&2
            exit 1
          fi
          if ! gh release view "$tag" >/dev/null 2>&1; then
            gh release create "$tag" --generate-notes --title "SoftEcho ${tag}" || true
          fi
          gh release upload "$tag" "${files[@]}" --clobber
